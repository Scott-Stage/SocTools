using Microsoft.Graph;            // GraphServiceClient
using Microsoft.Graph.Models;     // User model (v5)

var me   = await graphClient.Me.GetAsync();
var user = await graphClient.Users[userIdOrUpn].GetAsync();

var users = await graphClient.Users.GetAsync(cfg =>
{
    cfg.QueryParameters.Select = new[] { "id","displayName","userPrincipalName","mail" };
    cfg.QueryParameters.Top = 25;
});