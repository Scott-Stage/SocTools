window.aclParser = {
    getRelativePaths: function (input) {
        if (!input || !input.files) {
            return [];
        }

        return Array.from(input.files).map(function (file) {
            return file.webkitRelativePath || file.name;
        });
    }
};
