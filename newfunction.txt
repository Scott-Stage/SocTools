builder.Services.AddHttpClient("GraphApi", http =>
{
    http.BaseAddress = new Uri("https://graph.microsoft.com/v1.0/"); // <-- note the trailing slash
    http.DefaultRequestHeaders.Accept.ParseAdd("application/json");
});
