List<DeviceModel> devices =
    JsonSerializer.Deserialize<ResponseObject<DeviceModel>>(result,
        new JsonSerializerOptions { PropertyNameCaseInsensitive = true })
    ?.Resources ?? new List<DeviceModel>();