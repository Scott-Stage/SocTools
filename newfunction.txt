<MudDropContainer T="DeviceModel"
                  Items="@devices"
                  ItemDropped="@(async (MudItemDropInfo<DeviceModel> info) => await OnRowDropped(info))">
    <ChildContent>
        <MudDropZone T="DeviceModel" Identifier="rowZone" AllowReorder="true" />
    </ChildContent>

    <ItemRenderer Context="device">
        <MudPaper Class="d-flex justify-space-between pa-2">
            <div>@device.Hostname</div>
            <div>@(device.Online ? "Online" : "Offline")</div>
        </MudPaper>
    </ItemRenderer>
</MudDropContainer>

