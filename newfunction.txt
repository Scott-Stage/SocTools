index=zscaler
| eval query_domain=replace(url, "^https?://([^/]+).*", "\1")
| lookup top1m.csv domain AS query_domain OUTPUT domain as is_found
| where isnull(is_found)
| stats count by query_domain
| sort - count